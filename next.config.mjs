/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "media.valorant-api.com",
      "media.valorant-api.com",
    ],
  },
};

export default nextConfig;
